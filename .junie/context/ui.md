# UI Layer Rules

UI contains entry points.

Examples:

Controllers
CLI commands
HTTP requests

Controllers must remain thin.

Responsibilities:

- validate request
- call application use case
- return response
